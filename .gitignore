# Build outputs
**/build/
**/release/
**/debug/
*.o
*.obj
*.a
*.lib
*.so
*.dll
*.exe
*.dylib

# Duplicate core copies inside GUI tree (use top-level hashmonkey-core/ + contrib/prepare-gui-build.sh)
hashmonkey-gui/hashmonkey-core/
# Legacy upstream name — do not recreate; removed from HMNY build layout
hashmonkey-gui/monero/

# Logs and local artifacts
*.log
*.tar.gz
tags
TAGS
cscope.*

# Wallets and secrets — never commit
*.keys
*.wallet
*.seed
*.mnemonic
testnet-wallets/
testnet-bootstrap.log

# OS / IDE
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Personal / ops (keep local only — not in public repo)
BUILD_STATUS.md
TESTNET_README.txt
roadmap.txt
private/
docs/
infra/
scripts/
release-packages/

# Local deploy paths (optional personal scripts)
scripts/local/
scripts/*monkeynas*
scripts/deploy-*.ps1
scripts/deploy-*.sh
scripts/clean-*.ps1
scripts/organize-*.ps1
scripts/stage-*.ps1
scripts/package-*.ps1
scripts/build-win-static-monkeynas.sh
scripts/patch[0-9].py

# Release bundles (distribute separately)
release-windows/
HMNY_monero_fork/

# Local duplicates / archives (do not commit)
monero/
hmny-src.tgz
*.tgz
